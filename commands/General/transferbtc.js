module.exports = {
    name: "transferbtc",
    ownerOnly: false,
    run: async (client, message, args) => {

        const user = message.mentions.members.first();
        const amount = args[1];
        
    }
}